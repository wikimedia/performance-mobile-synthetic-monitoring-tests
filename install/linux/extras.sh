#!/bin/bash

# All installations are now done in the Docker container
# But if need something quick, we can add that in this file

# Exporting the Python that got the dependencies
export PYTHON=/usr/local/bin/python3